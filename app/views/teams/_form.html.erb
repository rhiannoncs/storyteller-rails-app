<%= form_for @team do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %><%= tag.br%>
	<%= f.label :membership_status %><%= tag.br %>
	<%= f.radio_button :open, 'true', :checked => true %>
	<%= label :open_true, 'Open' %>
	<%= f.radio_button :open, 'false' %>
	<%= label :open_false, 'Closed to New Members' %><%= tag.br %>

	<%= f.submit %>
<% end %>